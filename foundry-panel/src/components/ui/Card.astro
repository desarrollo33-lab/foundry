---
import { cn } from '@lib/utils/cn'

interface Props {
  hover?: boolean
  class?: string
}

const { hover = false, class: className } = Astro.props

const classes = cn(
  'expert',
  hover && 'hover:border-terminal-green',
  className
)
---

<article class={classes}>
  <slot />
</article>
